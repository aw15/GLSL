#version 330

layout(location=0) out vec4 FragColor;
in vec2 v_uv;

uniform int u_time;
uniform sampler2D u_TextureSlot;

void main()
{
	float tx = (v_uv.x);
	float ty = (v_uv.y)/6.0f+u_time/6.0f;
	FragColor = texture(u_TextureSlot, vec2(tx,ty));
} 