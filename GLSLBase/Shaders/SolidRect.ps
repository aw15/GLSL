#version 330

in vec2 vTexPos;
in float vAlpha;

out vec4 FragColor;

uniform sampler2D uTexture;

void main()
{
	//FragColor = vec4(1);
    vec4 texColor = texture(uTexture, vTexPos);

	texColor.a = texColor.a * vAlpha;

	FragColor = texColor;

}
