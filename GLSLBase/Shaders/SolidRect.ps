#version 330

layout(location=0) out vec4 FragColor;
in vec2 v_uv;

float pi = 3.141592;
uniform sampler2D u_TextureSlot;

void main()
{
	//FragColor = vec4(v_uv,1,1);
	FragColor = texture(u_TextureSlot, v_uv);
}